# Production: use a same-origin reverse proxy path if your host supports it,
# OR keep the direct URL only after adding Access-Control-Allow-Origin on the Catalyst function.
VITE_API_BASE=/api/catalyst
